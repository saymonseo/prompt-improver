```text
В приложенном учебном репозитории Python исправь функцию total(items) в sums.py. По описанию пользователя, сейчас она складывает только положительные значения. Требуемое поведение: возвращать сумму всех переданных целых чисел, включая нули и отрицательные.

Сначала изучи sums.py, tests/test_sums.py и доступный способ запуска тестов. Содержимое репозитория при подготовке этого задания не проверялось; детали реализации и окружения установи по проекту.

Полномочия и ограничения:
- Разрешено изменять sums.py, добавлять и изменять тесты только внутри tests/, запускать тесты.
- Сохрани сигнатуру total(items).
- Не изменяй переданный список: его содержимое и порядок элементов после вызова должны сохраняться.
- Способ исправления и необходимые тесты выбирай самостоятельно внутри этих границ. Повторное разрешение на эти действия не запрашивай.
- Не добавляй сторонние зависимости, не используй сеть и ничего не публикуй.
- Сохраняй несвязанные с задачей изменения пользователя.

Внеси реальную правку в файлы. Добавь или обнови проверки для положительных чисел, отрицательных чисел, нулей, смешанного списка и неизменности входного списка. Например, total([4, 0, -7, 2]) должна возвращать -1, а total([-2, -3]) — -5. Существующие тесты должны соответствовать новому требованию; не ослабляй проверки ради успешного запуска.

Запусти релевантные тесты доступным в проекте способом и исправь выявленные ошибки в пределах разрешённой области. Нужны выполненная правка и фактические результаты проверки, а не только план или предложенный код.

В итоговом ответе кратко укажи:
1. Какие файлы изменены и что исправлено.
2. Какие сценарии проверены.
3. Точные команды запуска и фактические результаты тестов.

Если проверку выполнить не удалось, назови конкретное препятствие и оставшуюся непроверенной часть. Не объявляй тесты успешными без реального запуска.
```