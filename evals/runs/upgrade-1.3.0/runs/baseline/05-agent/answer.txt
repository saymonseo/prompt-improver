```text
В приложенном учебном проекте Python исправь функцию total(items) в sums.py. По описанию пользователя, сейчас она складывает только положительные значения. Требуется возвращать сумму всех переданных целых чисел, включая нули и отрицательные.

Сохрани сигнатуру total(items). Переданный список должен оставаться неизменным: его содержимое и порядок элементов менять нельзя.

Полномочия и ограничения:
- Разрешено редактировать sums.py, добавлять и менять тесты внутри tests/, запускать тесты.
- Известный файл тестов: tests/test_sums.py.
- Способ исправления и необходимые проверки выбирай самостоятельно в этих границах. Повторное разрешение на эти действия не запрашивай.
- Не меняй файлы вне разрешённой области. Не добавляй сторонние зависимости, не используй сеть и ничего не публикуй.

Прочитай реализацию и существующие тесты. Команду запуска определи по доступным файлам проекта и локальному окружению. Внеси реальную правку и добавь или обнови тесты, проверяющие:
- сумму положительных чисел;
- смесь положительных, отрицательных чисел и нулей;
- только отрицательные числа;
- только нули;
- пустой список — результат 0;
- неизменность исходного списка после вызова.

Например, total([4, -7, 0, 2]) должна вернуть -1.

Фактически запусти тесты, включая существующие проверки проекта. Если обнаружатся сбои, связанные с исправлением, устрани их в разрешённых границах и повтори запуск.

В итоговом ответе кратко укажи изменённые файлы и суть исправления, точные команды запуска тестов и фактические результаты: сколько проверок прошло, какие завершились ошибкой или были пропущены. Если запуск заблокирован, назови конкретную причину и непроверенную часть. Не выдумывай результаты и не ограничивайся планом или предложением патча.
```